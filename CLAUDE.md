# CLAUDE.md

The frontend rulebook lives in `AGENTS.md` (the cross-tool convention). This file
exists only so Claude Code loads those rules natively — do not duplicate content
here; maintain `AGENTS.md` as the single source.

@AGENTS.md

Detailed specs: `docs/architecture.md`, `docs/design-system.md`,
`docs/api-coverage.md`, `docs/roadmap.md`. Project skills: `.claude/skills/`.
